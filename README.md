# xcb-opengl-template
Creates a green window using XCB and OpenGL, really simple.

## Build
 * clang
 * make
 * XCB development files
 * OpenGL development files
 
Run `make`, then `./build/xcb-opengl`
